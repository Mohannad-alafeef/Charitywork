import { TestimonialFilterPipe } from './testimonial-filter.pipe';

describe('TestimonialFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TestimonialFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
